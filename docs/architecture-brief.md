# RAG 智能购物助手 — 技术架构说明

> 面向产品经理的简洁版，不涉及代码细节。

---

## 一句话概括

用户用自然语言描述需求 → AI 助手多轮对话理解意图 → 从商品库中智能检索 → 跨平台比价 + 库存查询 → 推荐对比 → 一键下单。

---

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                  用户界面 (React + Vite)                             │
│                                                                     │
│   💬 聊天对话区 (左侧)                  📊 决策面板 (右侧可拖拽)      │
│   · 流式对话，实时逐字输出               · 四阶段进度条                │
│   · 商品推荐卡片 (3 张横滑)              · 用户需求标签                │
│   · 订单确认弹窗                        · 候选漏斗图                  │
│   · 快捷回复按钮                        · 跨平台比价表格              │
│   · 工具调用思考过程 (可展开)            · 大模型切换 (本地/云端)       │
└────────────────────────────┬────────────────────────────────────────┘
                             │  SSE (Server-Sent Events) 实时流式传输
┌────────────────────────────▼────────────────────────────────────────┐
│                    API 网关 (FastAPI)                                │
│                                                                     │
│   POST /api/chat/stream   流式对话（主接口）                         │
│   POST /api/chat          同步对话（备用）                           │
│   GET  /api/products      商品查询                                  │
│   GET  /api/health        健康检查                                  │
└───────┬─────────────────────────────────────────────────────────────┘
        │
┌───────▼─────────────────────────────────────────────────────────────┐
│                    AI Agent 大脑 (LangGraph ReAct)                   │
│                                                                     │
│   基于大语言模型，自主完成以下决策：                                   │
│   ① 判断用户意图，主动追问缺失信息，提供快捷回复选项                   │
│   ② 输出任务计划（task_plan），让用户看到接下来要做什么                 │
│   ③ 调用工具：搜索商品 → 跨平台比价 → 查库存                         │
│   ④ 从结果中筛选 3 款推荐，生成结构化推荐 JSON                        │
│   ⑤ 判断用户是否确认，展示订单详情，执行下单                          │
│                                                                     │
│   💡 不是固定流程脚本，是 AI 自主决策                                 │
└───────┬────────────┬──────────────┬──────────────┬──────────────────┘
        │            │              │              │
┌───────▼──────┐ ┌──▼───────┐ ┌───▼──────┐ ┌────▼──────┐
│ 🔍 语义检索   │ │ 💰 跨平台 │ │ 📦 库存   │ │ 🛒 下单    │
│ product_search│ │ 比价      │ │ 查询     │ │ place_order│
│              │ │ compare_  │ │ check_   │ │           │
│ 500条商品     │ │ prices    │ │ inventory│ │ 模拟下单   │
│ → 语义向量化  │ │           │ │          │ │           │
│ → Top-8 匹配 │ │ 京东/天猫/ │ │ 实时库存  │ │ 生成订单号 │
│              │ │ 拼多多     │ │ 数量查询  │ │           │
└──────────────┘ └──────────┘ └──────────┘ └───────────┘
```

---

## 核心能力

### 1. 多轮对话理解需求

不是关键词搜索，是真正理解自然语言：

| 用户说的 | 系统理解的 |
|---------|----------|
| "帮我找一款500以内的降噪耳机，通勤用" | 预算≤500、需要降噪、使用场景=通勤 |
| "入耳式，国产品牌优先" | 补充条件：入耳式、品牌偏好=国产 |
| "预算再低一点，300以内" | 更新预算≤300，其他条件保持 |

AI 在信息不足时会提供快捷回复按钮（如"入耳式""头戴式"），用户点击即可快速补充信息，无需手动输入。只需"预算+类型"或"预算+场景"即可开始搜索。

### 2. 语义检索（RAG）

传统搜索："降噪耳机" → 只匹配包含这四个字的商品

语义检索："通勤地铁上用的，不想听到噪音" → 理解意图，匹配所有降噪耳机

从 500 条商品中，根据语义相似度返回最匹配的 8 条，再由 AI 筛选出 3 条推荐。

### 3. 跨平台比价 + 库存查询

AI 自动为候选商品在京东、天猫、拼多多三大平台进行比价，同时查询实时库存，确保推荐的商品有货且价格最优。

### 4. 结构化推荐 + 下单

AI 以卡片形式展示 3 款商品对比（品牌、价格、评分、特点、优劣势、推荐理由），右侧决策面板同步展示候选漏斗和比价表格。用户点击选择 → 弹出订单确认 → 确认下单。

### 5. 实时流式交互

所有 AI 回复通过 SSE 逐字流式输出，工具调用过程实时展示（开始/结束），用户全程可见 AI 的"思考过程"。

---

## 技术选型

| 层级 | 技术 | 作用 | 为什么选它 |
|------|------|------|-----------|
| 大模型 | Qwen 2.5 (14B) | 理解意图、生成回复 | 中文能力强，支持本地运行 |
| Agent 框架 | LangGraph (ReAct) | 编排 AI 决策流程 | 业界主流，支持工具调用和流式事件 |
| 检索引擎 | LlamaIndex + Qdrant | 语义搜索商品 | 轻量、快速、内存模式适合 Demo |
| 向量模型 | BGE-small-zh-v1.5 | 将文本转为向量 | 中文专用、免费、本地运行 (~100MB) |
| 后端 | FastAPI | API 网关、SSE 流式传输 | 异步高性能，原生支持 SSE |
| 前端 | React 18 + Vite + Tailwind | 聊天界面 + 决策面板 | 组件化开发，热更新，响应式布局 |

---

## 数据规模

| 指标 | 数量 |
|------|------|
| 商品总数 | 500 条 |
| 品牌覆盖 | 32 个（Sony、Bose、华为、小米、JBL 等） |
| 价格区间 | ¥50 — ¥5,570 |
| 商品类型 | 入耳式、头戴式、骨传导、游戏耳机 |
| 使用场景 | 通勤、运动、办公、游戏、飞行等 |
| 平台覆盖 | 京东、天猫、拼多多（每件商品含跨平台价格） |

---

## 两种运行模式

| 模式 | 适用场景 | 成本 |
|------|---------|------|
| 本地模式 (Ollama) | 开发测试 | 免费，需要 16GB+ 显存 |
| 云端模式 (Qwen API) | 正式演示 | 按量付费，效果更好 |

页面顶部 Header 一键切换，无需重启。

---

## 前端组件架构

```
App (全局状态管理)
├── Header            LLM 模式切换、清空对话
├── ChatPanel (左侧)
│   ├── ChatMessage    单条消息：阶段标签、Markdown 渲染
│   │   ├── ThinkingSteps    工具调用过程（可展开/收起）
│   │   ├── ProductCard      商品推荐卡片（3张横滑）
│   │   ├── OrderConfirmModal 订单确认弹窗
│   │   └── QuickReplies     快捷回复按钮
│   └── ChatInput      输入框
└── DecisionPanel (右侧，可拖拽调整宽度)
    ├── StageProgress   四阶段进度条
    ├── UserProfile     用户需求标签展示
    ├── SearchFunnel    候选漏斗图（500→检索→筛选→3条）
    └── ComparisonBar   跨平台比价表格
```

---

## SSE 流式事件

前后端通过 Server-Sent Events 进行实时通信，事件类型包括：

| 事件类型 | 用途 |
|---------|------|
| `token` | LLM 输出的文本片段，逐字追加 |
| `tool_start` | 工具开始调用（搜索、比价、库存等） |
| `tool_end` | 工具调用完成，返回结果 |
| `task_plan` | 任务计划 JSON，前端展示步骤预览 |
| `structured_data` | 推荐方案 / 订单确认的结构化 JSON |
| `stage` | 当前工作流阶段变更 |
| `thinking_steps` | 工具调用汇总 |
| `quick_replies` | 快捷回复按钮选项 |
| `done` | 流结束标记 |

---

## 可扩展方向

- **接入真实电商 API**：替换模拟工具为京东/淘宝接口
- **增加商品品类**：目前仅耳机，可扩展至全品类
- **接入更多工具**：优惠券查询、物流追踪、价格监控
- **多模态**：支持图片搜索（"类似这款的耳机"）
- **用户画像**：记住用户偏好，个性化推荐
- **持久化存储**：当前 Qdrant 使用内存模式，可切换为持久化部署
